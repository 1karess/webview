# WebView API扫描工具 - 详细测试步骤

## 📋 测试目标

通过对比标准浏览器和不同WebView环境中的API差异，找出WebView额外注入的API，并分析这些API的安全风险。

---

## 🎯 测试方案

### 方案概述

1. **建立基准**：在标准浏览器中扫描，建立API基准
2. **WebView扫描**：在不同WebView中扫描，找出差异
3. **多网站对比**：在不同网站中扫描，看注入是否一致
4. **安全分析**：分析新增API的功能和安全风险

---

## 📝 详细测试步骤

### 阶段1：建立标准浏览器基准

#### 步骤1.1：准备标准浏览器环境

**操作**：
1. 打开Chrome浏览器（推荐）或Safari浏览器
2. 确保浏览器是最新版本
3. 打开无痕模式（避免扩展干扰）

**验证**：
- ✅ 浏览器版本正常
- ✅ 无痕模式已开启
- ✅ 没有安装可能影响测试的扩展

---

#### 步骤1.2：访问扫描工具

**操作**：
1. 在标准浏览器中打开扫描工具：
   ```
   https://webview-wheat-eight.vercel.app/tests/api-scanner.html
   ```

**验证**：
- ✅ 页面正常加载
- ✅ 显示"Chrome（标准浏览器）"或"Safari（标准浏览器）"
- ✅ 所有按钮可用

---

#### 步骤1.3：执行扫描

**操作**：
1. 点击"🚀 开始扫描"按钮
2. 等待扫描完成（通常需要几秒钟）
3. 观察进度条，等待达到100%

**验证**：
- ✅ 状态显示"正在扫描..."
- ✅ 进度条正常推进
- ✅ 扫描完成后显示"扫描完成"

---

#### 步骤1.4：检查扫描结果

**操作**：
1. 查看"所有API"标签页，确认有API列表
2. 查看统计信息：
   - 总API数量（应该在200-300个左右）
   - Bridge类型数量
   - 私有属性数量
   - 自定义对象数量
3. 查看详细日志，确认扫描过程正常

**验证**：
- ✅ API列表不为空
- ✅ 统计信息正常显示
- ✅ 日志显示扫描过程

---

#### 步骤1.5：导出基准数据

**操作**：
1. 点击"💾 导出数据"按钮
2. 文件会自动下载，文件名类似：`api-scan-Chrome（标准浏览器）-1234567890.json`
3. 保存这个文件，命名为：`baseline-chrome.json`（或`baseline-safari.json`）

**验证**：
- ✅ 文件成功下载
- ✅ 文件名包含浏览器类型和时间戳
- ✅ 文件已保存到本地

---

#### 步骤1.6：验证基准数据

**操作**：
1. 打开下载的JSON文件
2. 检查文件内容：
   - 应该有`timestamp`字段
   - 应该有`webview`字段（应该是"Chrome（标准浏览器）"或"Safari（标准浏览器）"）
   - 应该有`apis`数组，包含所有API
   - 应该有`categorized`对象，包含分类信息

**验证**：
- ✅ JSON文件格式正确
- ✅ 包含所有必要字段
- ✅ API数量合理（200-300个）

---

### 阶段2：在QQ WebView中扫描

#### 步骤2.1：准备QQ环境

**操作**：
1. 打开QQ APP
2. 确保QQ是最新版本
3. 准备一个可以分享链接的方式（比如通过文件传输助手）

**验证**：
- ✅ QQ APP正常打开
- ✅ 可以访问网页链接

---

#### 步骤2.2：在QQ中打开扫描工具

**操作**：
1. 在QQ中打开链接：
   ```
   https://webview-wheat-eight.vercel.app/tests/api-scanner.html
   ```
2. 等待页面加载完成

**验证**：
- ✅ 页面正常加载
- ✅ 显示"QQ WebView"
- ✅ 所有按钮可用

---

#### 步骤2.3：执行扫描

**操作**：
1. 点击"🚀 开始扫描"按钮
2. 等待扫描完成
3. 观察进度条和日志

**验证**：
- ✅ 扫描正常进行
- ✅ 扫描完成后显示结果

---

#### 步骤2.4：检查扫描结果

**操作**：
1. 查看"所有API"标签页
2. 查看统计信息，对比标准浏览器：
   - 总API数量应该更多（因为QQ注入了额外API）
   - Bridge类型数量应该增加
3. 查看"Bridge类型"标签页，查看QQ注入的Bridge
4. 查看"私有属性"标签页，查看私有属性（如`__mqqStartLoadTime`）

**验证**：
- ✅ API数量比标准浏览器多
- ✅ 发现了QQ特有的Bridge（如`TXWebKitNativeFetch`）
- ✅ 发现了私有属性

---

#### 步骤2.5：导出QQ扫描数据

**操作**：
1. 点击"💾 导出数据"按钮
2. 文件会自动下载，文件名类似：`api-scan-QQ WebView-1234567890.json`
3. 保存这个文件，命名为：`qq-webview-scan.json`

**验证**：
- ✅ 文件成功下载
- ✅ 文件已保存

---

#### 步骤2.6：对比差异（方法1：在工具中对比）

**操作**：
1. 在"导出/导入"标签页
2. 打开之前保存的基准数据文件（`baseline-chrome.json`）
3. 复制文件内容
4. 粘贴到"导入数据"文本框中
5. 点击"📤 导入数据"按钮
6. 查看详细日志，会显示对比结果

**验证**：
- ✅ 基准数据成功导入
- ✅ 日志显示对比结果
- ✅ 显示新增的API列表

---

#### 步骤2.7：对比差异（方法2：手动对比）

**操作**：
1. 打开基准数据文件（`baseline-chrome.json`）
2. 打开QQ扫描数据文件（`qq-webview-scan.json`）
3. 对比两个文件的`apis`数组
4. 找出QQ WebView中独有的API

**验证**：
- ✅ 成功找出差异
- ✅ 记录了新增的API

---

### 阶段3：多网站对比测试

#### 步骤3.1：选择测试网站

**建议测试的网站类型**：
1. **社交媒体**：
   - Facebook: `https://www.facebook.com`
   - Twitter: `https://www.twitter.com`
   - Reddit: `https://www.reddit.com`
2. **新闻网站**：
   - CNN: `https://www.cnn.com`
   - BBC: `https://www.bbc.com`
3. **你的测试网站**：
   - `https://webview-wheat-eight.vercel.app/tests/api-scanner.html`
4. **普通网站**：
   - Example: `https://example.com`

**选择至少5-10个不同类型的网站进行测试**

---

#### 步骤3.2：在每个网站中扫描

**对每个网站重复以下步骤**：

**操作**：
1. 在QQ中打开目标网站
2. 在地址栏输入扫描工具的URL（或通过书签访问）
3. 点击"🚀 开始扫描"
4. 等待扫描完成
5. 点击"💾 导出数据"
6. 保存文件，命名为：`qq-webview-{网站名}-scan.json`
   - 例如：`qq-webview-facebook-scan.json`
   - 例如：`qq-webview-reddit-scan.json`

**记录**：
- 网站名称
- 扫描时间
- 发现的API数量
- 是否有特殊的API

---

#### 步骤3.3：对比所有网站的扫描结果

**操作**：
1. 收集所有扫描结果文件
2. 对比所有文件的`apis`数组
3. 找出：
   - **全局API**：所有网站都有的API（QQ全局注入）
   - **选择性API**：只有某些网站有的API（QQ选择性注入）

**分析方法**：
```javascript
// 伪代码示例
const allScans = [scan1, scan2, scan3, ...];
const allAPIs = new Set();

// 收集所有API
allScans.forEach(scan => {
  scan.apis.forEach(api => allAPIs.add(api.name));
});

// 找出全局API（所有网站都有）
const globalAPIs = Array.from(allAPIs).filter(apiName => {
  return allScans.every(scan => 
    scan.apis.some(api => api.name === apiName)
  );
});

// 找出选择性API（只有某些网站有）
const selectiveAPIs = Array.from(allAPIs).filter(apiName => {
  const hasCount = allScans.filter(scan => 
    scan.apis.some(api => api.name === apiName)
  ).length;
  return hasCount > 0 && hasCount < allScans.length;
});
```

**记录结果**：
- 全局API列表
- 选择性API列表
- 每个网站特有的API

---

### 阶段4：安全分析

#### 步骤4.1：分析全局API

**操作**：
1. 列出所有全局API（所有网站都有的）
2. 对每个API进行分析：
   - 功能：这个API是做什么的？
   - 类型：Bridge、私有属性、自定义对象等
   - 安全风险：是否有安全风险？

**分析重点**：
- Bridge类型的API（如`TXWebKitNativeFetch`）
- 私有属性（如`__mqqStartLoadTime`）
- 自定义对象

**记录**：
- API名称
- 功能描述
- 安全风险等级（高/中/低）
- 潜在漏洞

---

#### 步骤4.2：分析选择性API

**操作**：
1. 列出所有选择性API（只有某些网站有的）
2. 分析注入规律：
   - 什么条件下会注入？
   - 是否有白名单机制？
   - 是否有黑名单机制？

**分析重点**：
- 注入条件（域名、网站类型等）
- 是否有绕过机制
- 安全影响

**记录**：
- API名称
- 注入条件
- 安全风险

---

#### 步骤4.3：测试API功能

**操作**：
1. 对发现的API进行功能测试：
   - 尝试调用函数（安全地）
   - 分析对象的属性和方法
   - 测试参数和返回值

**测试重点**：
- Bridge API的功能
- 是否有权限控制
- 是否有输入验证
- 是否泄露信息

**注意**：
- ⚠️ 测试时要小心，避免触发恶意行为
- ⚠️ 不要在生产环境中测试
- ⚠️ 记录所有测试结果

---

#### 步骤4.4：生成安全报告

**操作**：
1. 整理所有发现：
   - 全局API列表
   - 选择性API列表
   - 功能分析
   - 安全风险分析
2. 生成报告：
   - 发现的API数量
   - API分类
   - 安全风险等级
   - 建议

**报告格式**：
```markdown
# QQ WebView API安全分析报告

## 1. 概述
- 扫描时间：2024-01-01
- 测试网站数量：10
- 发现的API总数：XXX
- 新增API数量：XXX

## 2. 全局API（所有网站都有）
### 2.1 Bridge类型
- TXWebKitNativeFetch
  - 功能：网络请求，绕过CORS
  - 安全风险：高
  - 漏洞：可以访问任意URL，可能泄露用户信息

### 2.2 私有属性
- __mqqStartLoadTime
  - 功能：记录加载时间
  - 安全风险：低
  - 漏洞：可能泄露时间信息

## 3. 选择性API
### 3.1 特定网站注入
- 未发现（如果所有网站都一样）

## 4. 安全建议
- 建议1：限制Bridge的访问权限
- 建议2：加强输入验证
- 建议3：...
```

---

## 📊 测试记录表

### 标准浏览器基准

| 项目 | 值 |
|------|-----|
| 浏览器 | Chrome / Safari |
| 扫描时间 | YYYY-MM-DD HH:MM:SS |
| API总数 | XXX |
| Bridge数量 | XXX |
| 私有属性数量 | XXX |
| 文件名称 | baseline-chrome.json |

---

### QQ WebView扫描结果

| 网站 | 扫描时间 | API总数 | 新增API | Bridge数量 | 文件名称 |
|------|---------|--------|---------|-----------|----------|
| api-scanner.html | YYYY-MM-DD | XXX | XXX | XXX | qq-webview-scanner.json |
| facebook.com | YYYY-MM-DD | XXX | XXX | XXX | qq-webview-facebook.json |
| reddit.com | YYYY-MM-DD | XXX | XXX | XXX | qq-webview-reddit.json |
| twitter.com | YYYY-MM-DD | XXX | XXX | XXX | qq-webview-twitter.json |
| ... | ... | ... | ... | ... | ... |

---

### 对比分析结果

| 项目 | 结果 |
|------|------|
| 全局API数量 | XXX |
| 选择性API数量 | XXX |
| 所有网站都一致的API | XXX |
| 不同网站有差异的API | XXX |

---

## ⚠️ 注意事项

### 1. 测试环境

- ✅ 使用无痕模式（标准浏览器）
- ✅ 确保浏览器/APP是最新版本
- ✅ 避免安装可能影响测试的扩展

### 2. 数据保存

- ✅ 及时保存扫描结果
- ✅ 使用清晰的命名规则
- ✅ 备份重要数据

### 3. 安全测试

- ⚠️ 不要在生产环境中测试
- ⚠️ 测试时要小心，避免触发恶意行为
- ⚠️ 记录所有测试结果

### 4. 对比分析

- ✅ 确保对比的是同一个网站
- ✅ 注意时间差异（某些API可能动态注入）
- ✅ 多次测试，确保结果一致

---

## 🎯 预期结果

### 如果所有网站都一样

**说明**：
- QQ是全局注入API的
- 所有网站都能访问相同的API
- 安全风险是全局的

**下一步**：
- 重点分析这些全局API的安全风险
- 测试这些API的功能和漏洞

---

### 如果不同网站不同

**说明**：
- QQ有选择性注入机制
- 这是一个重要的安全研究点

**下一步**：
- 深入研究注入规律
- 找出注入条件
- 测试是否有绕过机制

---

## 📝 测试检查清单

### 阶段1：建立基准
- [ ] 在标准浏览器中打开扫描工具
- [ ] 执行扫描
- [ ] 检查扫描结果
- [ ] 导出基准数据
- [ ] 验证基准数据

### 阶段2：QQ WebView扫描
- [ ] 在QQ中打开扫描工具
- [ ] 执行扫描
- [ ] 检查扫描结果
- [ ] 导出扫描数据
- [ ] 对比差异

### 阶段3：多网站对比
- [ ] 选择至少5-10个网站
- [ ] 在每个网站中扫描
- [ ] 导出所有扫描结果
- [ ] 对比所有网站的结果
- [ ] 找出全局API和选择性API

### 阶段4：安全分析
- [ ] 分析全局API
- [ ] 分析选择性API
- [ ] 测试API功能
- [ ] 生成安全报告

---

## 💡 提示

1. **保存所有数据**：扫描结果很重要，及时保存
2. **多次测试**：确保结果一致
3. **详细记录**：记录所有发现和观察
4. **安全第一**：测试时要小心，避免触发恶意行为

---

## 📞 遇到问题？

如果遇到问题：
1. 检查浏览器/APP版本
2. 检查网络连接
3. 查看详细日志
4. 重新扫描

---

**祝测试顺利！** 🚀
