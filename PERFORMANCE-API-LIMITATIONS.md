# Performance API 在 WebView 中的限制分析

## 测试结果

### 第一次测试（成功）
- **时间**：2025-12-06 02:38:03
- **结果**：检测到了 `https://oauth.reddit.com/api/v1/me` 请求
- **状态**：用户已登录 Reddit（账号：karess）

### 后续测试（失败）
- **时间**：2025-12-06 02:46:49
- **结果**：未检测到任何 API 请求
- **状态**：用户已登录，但检测不到

## 为什么检测不到？

### 可能的原因

1. **Performance API 在 WebView 中的限制**
   - WebView 环境可能对 Performance API 有特殊限制
   - 跨域 iframe 的请求可能不会出现在父页面的 Performance API 中
   - 这是浏览器的安全机制

2. **请求时机问题**
   - API 请求可能在 iframe 加载前就已完成
   - Performance API 只记录已完成的请求
   - 如果请求在监听开始前就完成，可能检测不到

3. **请求方式问题**
   - Reddit 可能使用 Service Worker 或 WebSocket
   - 这些请求可能不会出现在 Performance API 的 `resource` 类型中
   - 需要检查其他类型的性能条目

4. **WebView 的特殊行为**
   - QQ WebView 可能对 Performance API 有特殊处理
   - 可能出于隐私保护，限制了跨域请求的可见性

## 这个发现的意义

### 1. **说明了 Performance API 的不确定性**

即使第一次测试成功检测到了 API 请求，但后续测试无法复现，这说明：
- Performance API 检测**不可靠**
- 不能作为稳定的检测方法
- 受环境、时机、浏览器实现等多种因素影响

### 2. **揭示了 WebView 的安全机制**

WebView 可能对 Performance API 有额外的限制：
- 保护用户隐私
- 防止侧信道攻击
- 限制跨域信息泄露

### 3. **对论文的价值**

这个发现本身就有价值：
- **证明了检测方法的局限性**
- **说明了为什么需要多种方法**
- **揭示了 WebView 安全机制的复杂性**

## 对比分析

| 测试场景 | 检测结果 | 可能原因 |
|---------|---------|---------|
| 第一次测试 | ✅ 检测到 API | 请求恰好在检测时发生 |
| 后续测试 | ❌ 未检测到 | 请求时机不同，或 WebView 限制 |
| 持续监听 | ❌ 未检测到 | Performance API 在 WebView 中受限 |

## 结论

### 核心发现

1. **Performance API 检测不可靠**
   - 第一次成功可能是偶然
   - 后续无法复现说明方法不稳定
   - 不能作为可靠的检测手段

2. **WebView 的安全机制**
   - 可能限制了 Performance API 的可见性
   - 这是保护用户隐私的措施
   - 但也说明了检测的困难

3. **需要其他方法**
   - 不能依赖 Performance API
   - 需要探索其他侧信道方法
   - 或者接受检测的局限性

### 对论文的建议

1. **如实记录**
   - 记录第一次成功检测到的情况
   - 也记录后续无法复现的情况
   - 说明检测方法的不确定性

2. **分析原因**
   - 分析为什么第一次能检测到
   - 分析为什么后续检测不到
   - 说明 WebView 安全机制的作用

3. **提出结论**
   - Performance API 检测**不可靠**
   - 需要其他检测方法
   - 或者说明检测的局限性

## 下一步

1. **尝试其他方法**
   - 方法14：加载时间分析
   - 方法11：URL 变化检测（虽然 Reddit 是 SPA）
   - 其他侧信道方法

2. **接受局限性**
   - 承认检测的困难
   - 说明这是正常的安全机制
   - 这也是论文的一部分

3. **记录发现**
   - 这个发现本身有价值
   - 说明了 WebView 安全机制的复杂性
   - 可以作为论文的讨论点

