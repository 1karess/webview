# iOS WebView 查看控制台的方法

## 问题

在 iOS 的 WebView（QQ、微信等 App 的内置浏览器）中，无法像 Safari 那样直接打开开发者工具查看控制台。

## 解决方案

### 方法 1：在页面上显示日志（最简单，推荐）

**原理**：把 `console.log` 的输出显示在页面上，而不是控制台。

**实现**：我已经在测试页面中添加了这个功能，但可以创建一个专门的日志显示工具。

### 方法 2：使用 Safari Web Inspector（需要 Mac）

**步骤**：
1. **在 Mac 上打开 Safari**
2. **启用开发菜单**：
   - Safari → 偏好设置 → 高级
   - 勾选"在菜单栏中显示开发菜单"
3. **连接 iPhone**：
   - 用 USB 线连接 iPhone 到 Mac
   - 在 iPhone 上：设置 → Safari → 高级 → Web 检查器（开启）
4. **在 Safari 中查看**：
   - Mac Safari → 开发 → [你的 iPhone] → [WebView 页面]
   - 会打开 Web Inspector，可以看到控制台

**限制**：
- 需要 Mac 和 USB 连接
- 只能查看 Safari 的 WebView，不能直接查看 QQ 的 WebView

### 方法 3：使用 Safari 远程调试（iOS 15+）

**步骤**：
1. **在 iPhone 上**：
   - 设置 → Safari → 高级 → Web 检查器（开启）
2. **在 Mac 上**：
   - 打开 Safari
   - 开发 → [你的 iPhone] → [页面]
   - 选择要调试的页面

**限制**：
- 只能调试 Safari，不能直接调试 QQ WebView
- 需要 iOS 15+ 和 Mac

### 方法 4：在页面上创建日志显示区域（最实用）

**我已经在测试页面中添加了这个功能**，但可以创建一个专门的工具：

创建一个页面，自动捕获所有 `console.log` 并显示在页面上。

### 方法 5：使用第三方工具

**工具**：
- **Eruda**：移动端调试工具，可以在页面上显示控制台
- **vConsole**：微信开发的移动端调试工具

**使用方法**：
在页面中引入这些工具，它们会在页面上显示一个控制台界面。

---

## 推荐方案：在页面上显示日志（最简单）

**所有测试页面都会自动显示结果在页面上，不需要控制台。**

**优点**：
- 不需要任何工具
- 不需要 Mac 或 USB 连接
- 直接在页面上看到结果
- 可以复制结果

**使用**：
- 所有测试页面（`bridge-audit-a.html`、`api-function-explorer.html` 等）都会在页面上显示结果
- 不需要控制台

---

## 如果还是需要控制台

### 对于 QQ WebView：

**QQ WebView 无法直接查看控制台**，但可以：

1. **使用页面日志显示**（推荐）
   - 所有测试结果都显示在页面上
   - 不需要控制台

2. **使用 Safari 远程调试**（如果页面也在 Safari 中打开）
   - 但 QQ WebView 和 Safari 是不同的环境
   - 可能看不到 QQ WebView 中的日志

3. **使用 Eruda 或 vConsole**（需要修改页面代码）
   - 在页面中引入这些工具
   - 会在页面上显示控制台

---

## 实际建议

**对于你的研究**：

1. **不需要控制台**：
   - 所有测试结果都显示在页面上
   - 可以直接看到结果
   - 可以复制结果

2. **如果需要调试**：
   - 使用页面上的日志显示
   - 或者使用 Eruda/vConsole 工具

3. **如果需要查看源码**：
   - 对于 JavaScript 函数，可以直接在页面上显示源码
   - 不需要控制台

